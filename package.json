{
  "name": "pcap-converter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:gst": "cd gst-recorder && rm -rf ./build && ./build.sh",
    "start": "ts-node ./src/server.ts",
    "tsc": "tsc --noEmit --skipLibCheck",
    "w": "tsc -w --noEmit"
  },
  "devDependencies": {
    "@types/node": "16",
    "typescript": "^4.2.4"
  }
}
